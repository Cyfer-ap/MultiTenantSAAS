import { PagePlaceholder } from '../components/PagePlaceholder'

export function UsersPage() {
    return (
        <PagePlaceholder
            title="Users"
            description="Manage tenant users, roles, statuses, and invitations."
        />
    )
}